# 🔧 Финальное исправление ошибки App.framework

## Проблема:
"Invalid Bundle. The bundle Runner.app/Frameworks/App.framework does not support the minimum OS Version specified in the Info.plist."

## Причина:
Flutter App.framework компилируется с минимальной версией iOS, которая не соответствует настройкам проекта.

## Решение:

Добавлена явная настройка `IPHONEOS_DEPLOYMENT_TARGET = 13.0` в конфигурационные файлы Flutter.

---

## Что было исправлено:

1. **ios/Flutter/Release.xcconfig:** Добавлено `IPHONEOS_DEPLOYMENT_TARGET = 13.0`
2. **ios/Flutter/Debug.xcconfig:** Добавлено `IPHONEOS_DEPLOYMENT_TARGET = 13.0`

Теперь Flutter будет компилировать App.framework с минимальной версией iOS 13.0.

---

## Что нужно сделать на Mac:

### 1. Обновите проект:
```bash
cd msngIosAndroidv2
git pull
```

### 2. Очистите и пересоберите:
```bash
# Очистите Flutter кэш
flutter clean

# Получите зависимости
flutter pub get

# Перейдите в ios
cd ios

# Очистите Pods
rm -rf Pods Podfile.lock .symlinks

# Переустановите Pods
pod install

# Вернитесь в корень
cd ..
```

### 3. В Xcode:

1. **Закройте Xcode** (если открыт)

2. **Откройте проект заново:**
   ```bash
   open ios/Runner.xcworkspace
   ```

3. **Проверьте настройки:**
   - Project → Runner → General → **iOS Deployment Target: 13.0**
   - Project → Runner → Build Settings → **iOS Deployment Target: 13.0**

4. **Очистите проект:**
   - Product → Clean Build Folder (Shift+Cmd+K)

5. **Создайте новый Archive:**
   - Product → Archive

6. **Загрузите билд:**
   - Distribute App → App Store Connect → Upload
   - При выборе опций: **СНИМИТЕ галочку** "Upload your app's symbols"
   - Выберите существующее приложение `com.vvedenskii.messenger`
   - Upload

---

## ⚠️ Про предупреждение CocoaPods:

Предупреждение о конфигурации CocoaPods не критично, но если хотите исправить:

В Xcode:
- Project → Runner → Build Settings
- Найдите "Base Configuration"
- Убедитесь, что для Release используется `Flutter/Release.xcconfig`
- Убедитесь, что для Debug используется `Flutter/Debug.xcconfig`

Или просто игнорируйте это предупреждение - оно не влияет на сборку.

---

## ✅ После исправления:

App.framework будет компилироваться с минимальной версией iOS 13.0, и ошибка "Invalid Bundle" должна исчезнуть!

---

## Проверка:

После загрузки билда проверьте в App Store Connect:
- TestFlight → Загрузка сборки
- Статус должен быть "Обработка" → "Обработка завершена"
- Билд появится в "Сборки iOS" со статусом "Готово к отправке"

---

## Если ошибка все еще появляется:

1. Убедитесь, что вы обновили проект (`git pull`)
2. Убедитесь, что выполнили `flutter clean` и `flutter pub get`
3. Убедитесь, что переустановили Pods (`pod install`)
4. Убедитесь, что очистили проект в Xcode (Clean Build Folder)
5. Создайте **НОВЫЙ** Archive (старый может быть с кэшированными настройками)
6. Проверьте в Xcode Build Settings, что iOS Deployment Target = 13.0 везде

---

## Готово! 🎉

После этих шагов билд должен успешно пройти валидацию!
