# 🔧 Исправление ошибок валидации билда

## Проблемы:

1. **Invalid Bundle:** Минимальная версия OS не поддерживается в App.framework
2. **Upload Symbols Failed:** Отсутствуют dSYM файлы для Agora фреймворков

---

## Решение 1: Обновление минимальной версии iOS

### Проблема:
App.framework требует более новую минимальную версию iOS, чем указано в проекте.

### Решение:

1. **В Xcode:**
   - Project → Runner → General
   - **iOS Deployment Target:** Установите **iOS 13.0** или выше
   - Или в Build Settings:
     - Project → Runner → Build Settings
     - Найдите "iOS Deployment Target"
     - Установите **13.0** или выше

2. **Проверьте Podfile:**
   - Убедитесь, что `platform :ios, '13.0'` установлено

3. **Переустановите Pods:**
   ```bash
   cd ios
   rm -rf Pods Podfile.lock
   pod install
   cd ..
   ```

---

## Решение 2: Отключение загрузки символов для Agora (Рекомендуется)

### Проблема:
Agora SDK не предоставляет dSYM файлы для всех своих фреймворков, что вызывает ошибки при загрузке символов.

### Решение:

**При загрузке билда в Xcode:**

1. **Distribute App → App Store Connect → Upload**

2. **На шаге выбора опций:**
   - ❌ **СНИМИТЕ галочку** с "Upload your app's symbols"
   - Или оставьте включенным, но ошибки символов не критичны для работы приложения

3. **Продолжите загрузку**

**Примечание:** Ошибки символов Agora не критичны - они используются только для отладки сбоев. Приложение будет работать нормально.

---

## Решение 3: Настройка генерации dSYM (Альтернатива)

Если хотите включить символы для отладки:

### В Xcode Build Settings:

1. **Project → Runner → Build Settings**

2. **Найдите "Debug Information Format":**
   - Для **Release** и **Profile:** Установите **"DWARF with dSYM File"**

3. **Найдите "Strip Debug Symbols During Copy":**
   - Установите **"No"** для Release

4. **Пересоберите Archive:**
   - Product → Clean Build Folder (Shift+Cmd+K)
   - Product → Archive

---

## Рекомендуемое решение:

### Шаг 1: Обновите минимальную версию iOS

**В Xcode:**
- Project → Runner → General → **iOS Deployment Target: 13.0**

**Или в Build Settings:**
- Project → Runner → Build Settings → **iOS Deployment Target: 13.0**

### Шаг 2: При загрузке билда

**Отключите загрузку символов:**
- Distribute App → App Store Connect → Upload
- На шаге опций: **СНИМИТЕ галочку** "Upload your app's symbols"
- Продолжите загрузку

### Шаг 3: Пересоберите

```bash
# Очистите проект
cd ios
rm -rf Pods Podfile.lock
pod install
cd ..

# В Xcode
# Product → Clean Build Folder (Shift+Cmd+K)
# Product → Archive
```

---

## Проверка после исправления:

1. ✅ iOS Deployment Target установлен на 13.0 или выше
2. ✅ При загрузке билда отключена загрузка символов (или ошибки символов игнорируются)
3. ✅ Archive создается без ошибок
4. ✅ Валидация проходит успешно

---

## Важно:

- **Ошибки символов Agora не критичны** - приложение будет работать нормально
- **Минимальная версия iOS должна быть 13.0+** для совместимости с Flutter и Agora SDK
- После исправления пересоберите Archive и загрузите заново

---

## После исправления:

Билд должен пройти валидацию успешно и появиться в TestFlight!
