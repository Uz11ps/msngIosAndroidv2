# 🔧 Исправление разрешений на iOS эмуляторе

## Проблема:
На эмуляторе iOS разрешения не запрашиваются и их нельзя включить в настройках.

## Решение:

Улучшена логика запроса разрешений - теперь они **всегда запрашиваются явно**, даже если статус не определен.

---

## Что было исправлено:

### 1. Запись голосовых сообщений:
- Теперь **всегда** вызывает `Permission.microphone.request()` перед записью
- Проверяет статус дважды (до и после запроса) для надежности

### 2. Звонки:
- Теперь **всегда** вызывает `Permission.microphone.request()` перед звонком
- Для видеозвонков **всегда** вызывает `Permission.camera.request()`
- Проверяет статус дважды для надежности

### 3. Выбор изображений:
- Добавлен запрос разрешения на доступ к галерее

---

## 📱 Важно для эмулятора iOS:

### Особенности эмулятора:

1. **Эмулятор может не поддерживать некоторые функции:**
   - Микрофон может работать не на всех эмуляторах
   - Камера может не работать на эмуляторе
   - Рекомендуется тестировать на реальном устройстве

2. **Разрешения в настройках эмулятора:**
   - Настройки → Messenger App → Разрешения
   - Раздел появится только после первого запроса разрешения
   - Если раздела нет, значит разрешение еще не запрашивалось

3. **Сброс разрешений на эмуляторе:**
   - Настройки → General → Reset → Reset Location & Privacy
   - Это сбросит все разрешения для всех приложений

---

## 🔄 Что нужно сделать:

### 1. Обновите проект:
```bash
cd msngIosAndroidv2
git pull
```

### 2. Пересоберите приложение:
```bash
flutter clean
flutter pub get
flutter run
```

### 3. Протестируйте на эмуляторе:

1. **Запустите приложение**
2. **Попробуйте записать голосовое сообщение:**
   - Нажмите кнопку микрофона в чате
   - Должен появиться системный диалог iOS с запросом разрешения
   - Нажмите "OK" или "Разрешить"

3. **Попробуйте сделать звонок:**
   - Нажмите кнопку звонка
   - Должен появиться системный диалог iOS с запросом разрешения
   - Нажмите "OK" или "Разрешить"

4. **Проверьте настройки:**
   - Настройки → Messenger App → Разрешения
   - Должны появиться переключатели для микрофона и камеры

---

## 🐛 Если разрешения все еще не запрашиваются:

### Проверка 1: Убедитесь, что Info.plist настроен правильно

В `ios/Runner/Info.plist` должны быть:
- ✅ `NSMicrophoneUsageDescription`
- ✅ `NSCameraUsageDescription`
- ✅ `NSPhotoLibraryUsageDescription`

### Проверка 2: Переустановите приложение

```bash
# Удалите приложение с эмулятора
# Затем пересоберите и запустите заново
flutter clean
flutter pub get
flutter run
```

### Проверка 3: Сбросьте разрешения эмулятора

1. Настройки → General → Reset → Reset Location & Privacy
2. Перезапустите приложение
3. Попробуйте использовать функцию снова

### Проверка 4: Проверьте логи

В консоли должны быть сообщения:
```
🎤 Microphone permission status: ...
🎤 Requesting microphone permission...
🎤 Microphone permission after request: ...
```

Если этих сообщений нет, значит код не выполняется.

---

## 📱 Тестирование на реальном устройстве:

**Рекомендуется тестировать разрешения на реальном iPhone**, так как:
- Эмулятор может не поддерживать микрофон/камеру
- На реальном устройстве разрешения работают надежнее
- TestFlight билд уже готов для тестирования

---

## ✅ После исправления:

Разрешения должны запрашиваться автоматически при первом использовании функции, как на эмуляторе, так и на реальном устройстве!
