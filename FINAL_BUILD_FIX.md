# ✅ Финальное исправление ошибки валидации

## Проблема решена!

Обновлен `MinimumOSVersion` в `AppFrameworkInfo.plist` с `12.0` на `13.0`.

---

## Что было исправлено:

- **ios/Flutter/AppFrameworkInfo.plist:** `MinimumOSVersion` изменен с `12.0` на `13.0`
- Теперь все настройки согласованы:
  - ✅ Podfile: `platform :ios, '13.0'`
  - ✅ project.pbxproj: `IPHONEOS_DEPLOYMENT_TARGET = 13.0`
  - ✅ AppFrameworkInfo.plist: `MinimumOSVersion = 13.0`

---

## Что нужно сделать на Mac:

### 1. Обновите проект:
```bash
cd msngIosAndroidv2
git pull
```

### 2. Очистите и пересоберите:
```bash
cd ios
rm -rf Pods Podfile.lock
pod install
cd ..
flutter clean
flutter pub get
```

### 3. В Xcode:

1. **Откройте проект:**
   ```bash
   open ios/Runner.xcworkspace
   ```

2. **Проверьте настройки:**
   - Project → Runner → General → **iOS Deployment Target: 13.0**
   - Project → Runner → Build Settings → **iOS Deployment Target: 13.0**

3. **Очистите проект:**
   - Product → Clean Build Folder (Shift+Cmd+K)

4. **Создайте новый Archive:**
   - Product → Archive

5. **Загрузите билд:**
   - Distribute App → App Store Connect → Upload
   - При выборе опций: **СНИМИТЕ галочку** "Upload your app's symbols" (ошибки символов Agora не критичны)
   - Выберите существующее приложение `com.vvedenskii.messenger`
   - Upload

---

## ⚠️ Про ошибки символов Agora:

Ошибки **"Upload Symbols Failed"** для Agora фреймворков **НЕ критичны**:
- Приложение будет работать нормально
- Символы используются только для отладки сбоев
- Можно либо отключить загрузку символов, либо игнорировать эти ошибки

---

## ✅ После исправления:

Билд должен пройти валидацию успешно! Ошибка "Invalid Bundle" больше не должна появляться.

---

## Проверка:

После загрузки билда проверьте в App Store Connect:
- TestFlight → Загрузка сборки
- Статус должен быть "Обработка" → "Обработка завершена"
- Билд появится в "Сборки iOS" со статусом "Готово к отправке"

---

## Если ошибка все еще появляется:

1. Убедитесь, что вы обновили проект (`git pull`)
2. Убедитесь, что переустановили Pods (`pod install`)
3. Убедитесь, что очистили проект (`flutter clean`)
4. Проверьте в Xcode, что iOS Deployment Target = 13.0 везде
5. Создайте новый Archive (старый может быть с кэшированными настройками)

---

## Готово! 🎉

После этих шагов билд должен успешно пройти валидацию и появиться в TestFlight!
