# 🔧 Финальное исправление минимальной версии iOS для App.framework

## Проблема:
"Invalid Bundle. The bundle Runner.app/Frameworks/App.framework does not support the minimum OS Version specified in the Info.plist."

App.framework все еще компилируется с неправильной минимальной версией iOS.

## Решение:

Нужно явно установить минимальную версию iOS в Build Settings проекта для всех конфигураций.

---

## Что нужно сделать на Mac:

### Вариант 1: Через Xcode (Рекомендуется)

1. **Откройте проект:**
   ```bash
   open ios/Runner.xcworkspace
   ```

2. **В Xcode:**
   - Project → Runner → **Build Settings**
   - В поиске введите: **"iOS Deployment Target"**
   - Найдите **"iOS Deployment Target"** в разделе **"Deployment"**
   - Установите **13.0** для всех конфигураций:
     - Debug: **13.0**
     - Release: **13.0**
     - Profile: **13.0**

3. **Также проверьте:**
   - Project → Runner → **General** → **iOS Deployment Target: 13.0**

4. **Очистите проект:**
   - Product → Clean Build Folder (Shift+Cmd+K)

5. **Создайте новый Archive:**
   - Product → Archive

### Вариант 2: Через командную строку (Альтернатива)

Если Xcode не помогает, попробуйте собрать через Flutter CLI:

```bash
cd msngIosAndroidv2

# Очистите проект
flutter clean

# Соберите IPA напрямую
flutter build ipa --release

# Файл будет в: build/ios/ipa/messenger_app.ipa
```

Затем загрузите `.ipa` файл через:
- Xcode → Window → Organizer → Archives → Distribute App
- Или через Transporter app от Apple

---

## Проверка настроек в Xcode:

### Build Settings:

1. Project → Runner → Build Settings
2. Найдите "iOS Deployment Target"
3. Убедитесь, что установлено **13.0** для:
   - Debug
   - Release  
   - Profile

### General:

1. Project → Runner → General
2. Убедитесь, что **iOS Deployment Target: 13.0**

### Info.plist:

Проверьте, что в `ios/Runner/Info.plist` нет явного указания минимальной версии (она берется из Build Settings).

---

## Если ошибка все еще появляется:

### Попробуйте увеличить минимальную версию до 14.0:

Иногда Flutter требует более новую версию iOS:

1. **В Xcode:**
   - Project → Runner → Build Settings → iOS Deployment Target: **14.0**
   - Project → Runner → General → iOS Deployment Target: **14.0**

2. **Обновите Podfile:**
   ```ruby
   platform :ios, '14.0'
   ```

3. **Обновите project.pbxproj:**
   - Все `IPHONEOS_DEPLOYMENT_TARGET = 13.0` → `14.0`

4. **Обновите AppFrameworkInfo.plist:**
   - `MinimumOSVersion` → `14.0`

5. **Переустановите Pods:**
   ```bash
   cd ios
   rm -rf Pods Podfile.lock
   pod install
   cd ..
   ```

6. **Очистите и пересоберите:**
   ```bash
   flutter clean
   flutter pub get
   ```

---

## Альтернативное решение: Использовать Flutter build команду

Попробуйте собрать через Flutter CLI вместо Xcode:

```bash
cd msngIosAndroidv2

# Очистите
flutter clean
flutter pub get

# Соберите IPA
flutter build ipa --release

# Загрузите через Xcode Organizer или Transporter
```

---

## ✅ После исправления:

1. Убедитесь, что iOS Deployment Target = 13.0 (или 14.0) везде
2. Очистите проект
3. Создайте новый Archive
4. Загрузите билд

---

## Важно:

- **Создайте НОВЫЙ Archive** после изменения настроек
- Старые архивы могут содержать кэшированные настройки
- Убедитесь, что все настройки согласованы (13.0 или 14.0 везде)

---

## Если ничего не помогает:

Попробуйте увеличить минимальную версию iOS до **14.0** или даже **15.0** - это может решить проблему с совместимостью Flutter и Agora SDK.
