# 🔧 Исправление: Info.plist не включен в сборку

## Проблема:
Все разрешения возвращают `PermissionStatus.permanentlyDenied`, и в настройках iOS нет переключателей для микрофона, камеры и фото.

## Причина:
`Info.plist` может быть не правильно включен в сборку Xcode, или ключи разрешений не читаются iOS.

---

## ✅ Решение:

### Шаг 1: Проверьте Info.plist в Xcode

1. Откройте проект в Xcode:
```bash
cd ios
open Runner.xcworkspace
```

2. Выберите `Runner` в навигаторе слева (синяя иконка вверху)

3. Выберите вкладку `Build Settings`

4. Найдите `INFOPLIST_FILE` в поиске

5. Убедитесь, что значение: `Runner/Info.plist`

6. Если значение неправильное - измените его на `Runner/Info.plist`

### Шаг 2: Проверьте Info.plist в Info вкладке

1. В Xcode выберите `Runner` → вкладка `Info`

2. Убедитесь, что есть следующие ключи:
   - ✅ `Privacy - Microphone Usage Description` (NSMicrophoneUsageDescription)
   - ✅ `Privacy - Camera Usage Description` (NSCameraUsageDescription)
   - ✅ `Privacy - Photo Library Usage Description` (NSPhotoLibraryUsageDescription)
   - ✅ `Privacy - Photo Library Add Usage Description` (NSPhotoLibraryAddUsageDescription)

3. Если ключей нет - добавьте их:
   - Нажмите "+" рядом с "Custom iOS Target Properties"
   - Выберите нужный ключ из списка
   - Введите описание на русском языке

### Шаг 3: Проверьте, что Info.plist включен в Copy Bundle Resources

1. В Xcode выберите `Runner` → вкладка `Build Phases`

2. Разверните `Copy Bundle Resources`

3. Убедитесь, что `Info.plist` там есть

4. Если нет - нажмите "+" и добавьте `Info.plist`

### Шаг 4: Удалите приложение полностью

**На эмуляторе:**
- Долгим нажатием на иконку → Удалить приложение

**Или через команду:**
```bash
xcrun simctl uninstall booted com.vvedenskii.messenger
```

### Шаг 5: Сбросьте разрешения эмулятора

```bash
# Сбросьте все разрешения эмулятора
xcrun simctl privacy booted reset all

# Или сбросьте только для вашего приложения
xcrun simctl privacy booted reset all com.vvedenskii.messenger
```

### Шаг 6: Очистите и пересоберите проект

```bash
cd msngIosAndroidv2

# Очистите проект
flutter clean

# Получите зависимости
flutter pub get

# Очистите iOS зависимости
cd ios
rm -rf Pods Podfile.lock DerivedData
pod install
cd ..

# Очистите Xcode Derived Data
rm -rf ~/Library/Developer/Xcode/DerivedData/*
```

### Шаг 7: Пересоберите через Xcode

1. В Xcode: Product → Clean Build Folder (Shift+Cmd+K)

2. Product → Build (Cmd+B)

3. Проверьте, что нет ошибок

### Шаг 8: Запустите приложение

```bash
flutter run
```

Или через Xcode: Product → Run (Cmd+R)

### Шаг 9: При первом использовании функции

Когда вы впервые:
- Нажмете кнопку звонка → должен появиться системный диалог iOS
- Нажмете кнопку записи голосового сообщения → должен появиться системный диалог iOS
- Нажмете кнопку видеозвонка → должны появиться диалоги для микрофона и камеры
- Нажмете кнопку выбора изображения → должен появиться системный диалог iOS

---

## 🔍 Альтернативное решение: Проверьте файл Info.plist напрямую

Убедитесь, что файл `ios/Runner/Info.plist` содержит правильные ключи:

```xml
<key>NSMicrophoneUsageDescription</key>
<string>Приложению необходим доступ к микрофону для записи голосовых сообщений и аудиозвонков</string>

<key>NSCameraUsageDescription</key>
<string>Приложению необходим доступ к камере для отправки фотографий и видеозвонков</string>

<key>NSPhotoLibraryUsageDescription</key>
<string>Приложению необходим доступ к галерее для выбора фотографий</string>

<key>NSPhotoLibraryAddUsageDescription</key>
<string>Приложению необходим доступ для сохранения фотографий в галерею</string>
```

---

## 📱 Если проблема сохраняется:

### Вариант 1: Используйте реальное устройство

На реальном iPhone разрешения работают надежнее:
1. Подключите iPhone к Mac
2. Доверьтесь компьютеру на iPhone
3. Запустите:
```bash
flutter run -d <your-iphone-id>
```

### Вариант 2: Проверьте версию iOS

Убедитесь, что эмулятор использует iOS 14.0 или выше (в `Podfile` указано `platform :ios, '14.0'`).

---

## ✅ После успешной установки:

После переустановки приложения с правильным `Info.plist`:
1. При первом использовании функции появится системный диалог iOS
2. После предоставления разрешения, оно появится в настройках iOS
3. Вы сможете управлять разрешениями через Настройки → Messenger App
