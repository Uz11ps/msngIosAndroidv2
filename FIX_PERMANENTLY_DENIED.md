# 🔧 Исправление проблемы с разрешениями (permanentlyDenied)

## Проблема:
- Разрешения возвращают статус `PermissionStatus.permanentlyDenied`
- В настройках iOS нет переключателей для микрофона, камеры и фото
- Системный диалог iOS не появляется

## Причина:
Приложение было установлено **до того, как были добавлены ключи разрешений** в `Info.plist`. iOS не показывает разрешения в настройках, если они не были запрошены хотя бы раз с правильными ключами в `Info.plist`.

---

## ✅ Решение:

### Шаг 1: Удалите приложение полностью

**На эмуляторе:**
- Долгим нажатием на иконку приложения → Удалить приложение
- Или через Xcode: Window → Devices and Simulators → выберите устройство → удалите приложение

**На реальном устройстве:**
- Долгим нажатием на иконку → Удалить приложение

### Шаг 2: Сбросьте разрешения эмулятора

```bash
# Сбросьте все разрешения эмулятора
xcrun simctl privacy booted reset all

# Или сбросьте только для вашего приложения
xcrun simctl privacy booted reset all com.vvedenskii.messenger
```

### Шаг 3: Убедитесь, что Info.plist правильно настроен

Проверьте файл `ios/Runner/Info.plist` - должны быть следующие ключи:

```xml
<key>NSMicrophoneUsageDescription</key>
<string>Приложению необходим доступ к микрофону для записи голосовых сообщений и аудиозвонков</string>

<key>NSCameraUsageDescription</key>
<string>Приложению необходим доступ к камере для отправки фотографий и видеозвонков</string>

<key>NSPhotoLibraryUsageDescription</key>
<string>Приложению необходим доступ к галерее для выбора фотографий</string>

<key>NSPhotoLibraryAddUsageDescription</key>
<string>Приложению необходим доступ для сохранения фотографий в галерею</string>
```

### Шаг 4: Проверьте Info.plist в Xcode

1. Откройте проект в Xcode:
```bash
cd ios
open Runner.xcworkspace
```

2. Выберите `Runner` в навигаторе слева
3. Выберите вкладку `Info`
4. Убедитесь, что есть следующие ключи:
   - ✅ `Privacy - Microphone Usage Description` (NSMicrophoneUsageDescription)
   - ✅ `Privacy - Camera Usage Description` (NSCameraUsageDescription)
   - ✅ `Privacy - Photo Library Usage Description` (NSPhotoLibraryUsageDescription)
   - ✅ `Privacy - Photo Library Add Usage Description` (NSPhotoLibraryAddUsageDescription)

5. Если ключей нет - добавьте их вручную через Xcode:
   - Нажмите "+" рядом с "Custom iOS Target Properties"
   - Выберите нужный ключ из списка
   - Введите описание

### Шаг 5: Очистите и пересоберите проект

```bash
cd msngIosAndroidv2

# Очистите проект
flutter clean

# Получите зависимости
flutter pub get

# Пересоберите iOS зависимости
cd ios
rm -rf Pods Podfile.lock
pod install
cd ..

# Пересоберите и запустите
flutter run
```

### Шаг 6: При первом использовании функции

Когда вы впервые:
- Нажмете кнопку звонка → должен появиться системный диалог iOS с запросом разрешения на микрофон
- Нажмете кнопку записи голосового сообщения → должен появиться системный диалог iOS
- Нажмете кнопку видеозвонка → должны появиться диалоги для микрофона и камеры
- Нажмете кнопку выбора изображения → должен появиться системный диалог iOS для доступа к галерее

---

## 🔍 Если разрешения все еще не появляются в настройках:

### Вариант 1: Проверьте, что Info.plist включен в сборку

1. В Xcode выберите `Runner` → `Build Phases`
2. Найдите `Copy Bundle Resources`
3. Убедитесь, что `Info.plist` там есть
4. Если нет - добавьте его

### Вариант 2: Проверьте Build Settings

1. В Xcode выберите `Runner` → `Build Settings`
2. Найдите `INFOPLIST_FILE`
3. Убедитесь, что путь правильный: `Runner/Info.plist`

### Вариант 3: Используйте реальное устройство

На реальном iPhone разрешения работают надежнее:
1. Подключите iPhone к Mac
2. Доверьтесь компьютеру на iPhone
3. Запустите:
```bash
flutter run -d <your-iphone-id>
```

---

## 📱 После успешной установки:

После переустановки приложения с правильным `Info.plist`:
1. При первом использовании функции появится системный диалог iOS
2. После предоставления разрешения, оно появится в настройках iOS
3. Вы сможете управлять разрешениями через Настройки → Messenger App

---

## ✅ Улучшения в коде:

Код теперь:
- ✅ Правильно обрабатывает статус `permanentlyDenied`
- ✅ Показывает более понятные сообщения пользователю
- ✅ Автоматически открывает настройки приложения при необходимости
