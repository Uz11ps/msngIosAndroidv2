# 🔧 Исправление: Разрешения не появляются в настройках iOS

## Проблема:
Разрешения на микрофон, камеру и фото не появляются в настройках iOS, появляется только разрешение на локальную сеть.

## Причина:
iOS не видит разрешения в сборке приложения, даже если они есть в `Info.plist`. Нужно добавить их через Xcode UI.

---

## ✅ Решение:

### Шаг 1: Откройте проект в Xcode

```bash
cd ios
open Runner.xcworkspace
```

### Шаг 2: Добавьте разрешения через Xcode UI

1. **Выберите `Runner`** в навигаторе слева (синяя иконка вверху)

2. **Выберите вкладку `Info`** (не Build Settings)

3. **Проверьте, что есть следующие ключи:**
   - `Privacy - Microphone Usage Description`
   - `Privacy - Camera Usage Description`
   - `Privacy - Photo Library Usage Description`
   - `Privacy - Photo Library Add Usage Description`

4. **Если ключей нет или они пустые:**
   - Нажмите **"+"** рядом с "Custom iOS Target Properties"
   - Выберите нужный ключ из списка:
     - `Privacy - Microphone Usage Description` (NSMicrophoneUsageDescription)
     - `Privacy - Camera Usage Description` (NSCameraUsageDescription)
     - `Privacy - Photo Library Usage Description` (NSPhotoLibraryUsageDescription)
     - `Privacy - Photo Library Add Usage Description` (NSPhotoLibraryAddUsageDescription)
   - Введите описание на русском языке:
     - Микрофон: `Приложению необходим доступ к микрофону для записи голосовых сообщений и аудиозвонков`
     - Камера: `Приложению необходим доступ к камере для отправки фотографий и видеозвонков`
     - Фото (чтение): `Приложению необходим доступ к галерее для выбора фотографий`
     - Фото (запись): `Приложению необходим доступ для сохранения фотографий в галерею`

5. **Сохраните изменения** (Cmd+S)

### Шаг 3: Проверьте Build Settings

1. Выберите `Runner` → вкладка `Build Settings`

2. Найдите `INFOPLIST_FILE` в поиске

3. Убедитесь, что значение: `Runner/Info.plist`

4. Если значение неправильное - измените его на `Runner/Info.plist`

5. Убедитесь, что `GENERATE_INFOPLIST_FILE` = `NO` (для основного таргета Runner, не для тестов)

### Шаг 4: Очистите проект

В Xcode:
- **Product → Clean Build Folder** (Shift+Cmd+K)

Или через терминал:
```bash
cd msngIosAndroidv2
flutter clean
rm -rf ios/Pods ios/Podfile.lock
rm -rf ~/Library/Developer/Xcode/DerivedData/*
```

### Шаг 5: Пересоберите зависимости

```bash
cd ios
pod install
cd ..
flutter pub get
```

### Шаг 6: Удалите приложение полностью

```bash
# Удалите приложение с эмулятора
xcrun simctl uninstall booted com.vvedenskii.messenger

# Сбросьте разрешения
xcrun simctl privacy booted reset all
```

### Шаг 7: Пересоберите через Xcode

1. В Xcode: **Product → Build** (Cmd+B)
2. Проверьте, что нет ошибок
3. **Product → Run** (Cmd+R)

Или через Flutter:
```bash
flutter run
```

### Шаг 8: Проверьте разрешения

После запуска приложения:

1. Откройте **Настройки** на эмуляторе/устройстве
2. Найдите **Messenger App**
3. Должны появиться переключатели для:
   - ✅ Микрофон
   - ✅ Камера
   - ✅ Фото

---

## 🔍 Если разрешения все еще не появляются:

### Вариант 1: Проверьте, что Info.plist включен в Copy Bundle Resources

1. В Xcode выберите `Runner` → вкладка `Build Phases`
2. Разверните `Copy Bundle Resources`
3. Убедитесь, что `Info.plist` там есть
4. Если нет - нажмите "+" и добавьте `Info.plist`

### Вариант 2: Проверьте собранный Info.plist

После сборки проверьте, что разрешения есть в собранном приложении:

```bash
# Найдите путь к собранному приложению
# Обычно это: ~/Library/Developer/Xcode/DerivedData/Runner-*/Build/Products/Debug-iphonesimulator/Runner.app

# Проверьте Info.plist в собранном приложении
plutil -p ~/Library/Developer/Xcode/DerivedData/Runner-*/Build/Products/Debug-iphonesimulator/Runner.app/Info.plist | grep -i "microphone\|camera\|photo"
```

Должны быть видны ключи:
- `NSMicrophoneUsageDescription`
- `NSCameraUsageDescription`
- `NSPhotoLibraryUsageDescription`
- `NSPhotoLibraryAddUsageDescription`

### Вариант 3: Используйте реальное устройство

На реальном iPhone разрешения работают надежнее:
1. Подключите iPhone к Mac
2. Доверьтесь компьютеру на iPhone
3. Запустите через Xcode или `flutter run -d <your-iphone-id>`

---

## ✅ После исправления:

После добавления разрешений через Xcode UI и переустановки приложения:
1. Разрешения должны появиться в настройках iOS
2. При первом использовании функции должен появиться системный диалог iOS
3. Вы сможете управлять разрешениями через Настройки → Messenger App
