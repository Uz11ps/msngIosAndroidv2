# 🚀 Сборка через Flutter CLI вместо Xcode Archive

## Проблема:
Xcode Archive все еще создает App.framework с неправильной минимальной версией iOS.

## Решение:
Использовать `flutter build ipa` напрямую - это гарантирует правильную компиляцию App.framework с нужными настройками.

---

## 📋 Пошаговая инструкция:

### Шаг 1: Обновите проект
```bash
cd msngIosAndroidv2
git pull
```

### Шаг 2: Очистите проект
```bash
flutter clean
flutter pub get
```

### Шаг 3: Переустановите Pods
```bash
cd ios
rm -rf Pods Podfile.lock .symlinks
pod install
cd ..
```

### Шаг 4: Соберите IPA через Flutter CLI
```bash
flutter build ipa --release
```

Эта команда:
- Правильно скомпилирует App.framework с минимальной версией iOS 14.0
- Создаст архив с правильными настройками
- Сгенерирует `.ipa` файл готовый для загрузки

### Шаг 5: Загрузите IPA файл

После сборки файл будет в: `build/ios/ipa/messenger_app.ipa`

**Вариант A: Через Xcode Organizer**
1. Откройте Xcode
2. Window → Organizer (или Cmd+Shift+9)
3. Нажмите "Distribute App" или перетащите `.ipa` файл
4. Выберите "App Store Connect" → "Upload"
5. Выберите существующее приложение `com.vvedenskii.messenger`
6. Upload

**Вариант B: Через Transporter App**
1. Скачайте Transporter app из App Store (если еще нет)
2. Откройте Transporter
3. Перетащите `.ipa` файл в окно
4. Нажмите "Deliver"
5. Войдите в Apple ID
6. Загрузите

---

## ✅ Преимущества Flutter build ipa:

- ✅ Правильно компилирует App.framework с нужными настройками
- ✅ Использует настройки из Podfile и xcconfig файлов
- ✅ Избегает проблем с кэшированием Xcode
- ✅ Более надежный способ сборки для Flutter проектов

---

## 🔍 Если flutter build ipa не работает:

### Проверьте версию Flutter:
```bash
flutter --version
flutter doctor
```

### Убедитесь, что Xcode настроен:
```bash
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
```

### Проверьте подписку:
- Xcode → Preferences → Accounts
- Убедитесь, что ваш Apple ID добавлен
- Выберите правильную Team

---

## 📝 Альтернатива: Исправить в Xcode напрямую

Если flutter build ipa не помогает, попробуйте в Xcode:

1. **Project → Runner → Build Settings**
2. Найдите **"iOS Deployment Target"**
3. Установите **14.0** для ВСЕХ конфигураций:
   - Debug: 14.0
   - Release: 14.0
   - Profile: 14.0

4. **Также проверьте:**
   - Project → Runner → General → iOS Deployment Target: 14.0

5. **Очистите Derived Data:**
   - Xcode → Preferences → Locations
   - Нажмите стрелку рядом с "Derived Data"
   - Удалите папку для вашего проекта
   - Или удалите всю папку Derived Data

6. **Очистите проект:**
   - Product → Clean Build Folder (Shift+Cmd+K)

7. **Создайте новый Archive**

---

## 🎯 Рекомендуемый способ:

**Используйте `flutter build ipa`** - это самый надежный способ для Flutter проектов!

```bash
cd msngIosAndroidv2
flutter clean
flutter pub get
cd ios && pod install && cd ..
flutter build ipa --release
```

Затем загрузите `.ipa` файл через Xcode Organizer или Transporter.

---

## ✅ После успешной сборки:

Билд должен пройти валидацию без ошибки "Invalid Bundle"!
