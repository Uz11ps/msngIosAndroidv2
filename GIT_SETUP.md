# 🚀 Быстрая передача проекта на Mac через Git

## ✅ Git репозиторий уже создан!

Теперь выполните следующие шаги:

---

## 📝 Шаг 1: Добавьте файлы в Git

```bash
cd c:\Users\1\Desktop\cursor\msngIosAndroidv2
git add .
git commit -m "Initial commit - готово для iOS сборки"
```

---

## 🌐 Шаг 2: Создайте репозиторий на GitHub

1. **Зайдите на GitHub:**
   - https://github.com
   - Войдите в свой аккаунт (или создайте новый)

2. **Создайте новый репозиторий:**
   - Нажмите "+" в правом верхнем углу
   - Выберите "New repository"
   - **Repository name:** `msngIosAndroidv2`
   - **Description:** `Messenger App для iOS и Android`
   - Выберите **"Private"** (если не хотите публичный доступ)
   - **НЕ** добавляйте README, .gitignore, license (они уже есть)
   - Нажмите **"Create repository"**

3. **Скопируйте URL репозитория:**
   - Будет что-то вроде: `https://github.com/ВАШ_USERNAME/msngIosAndroidv2.git`

---

## ⬆️ Шаг 3: Заливайте код на GitHub

```bash
# Замените ВАШ_USERNAME на ваш GitHub username
git remote add origin https://github.com/ВАШ_USERNAME/msngIosAndroidv2.git
git branch -M main
git push -u origin main
```

Если попросит авторизацию:
- Используйте Personal Access Token (не пароль)
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)

---

## 💻 Шаг 4: На Mac - клонируйте проект

```bash
# Замените ВАШ_USERNAME на ваш GitHub username
git clone https://github.com/ВАШ_USERNAME/msngIosAndroidv2.git
cd msngIosAndroidv2
```

---

## 🔧 Шаг 5: Установите зависимости на Mac

```bash
# Flutter зависимости
flutter pub get

# iOS зависимости
cd ios
pod install
cd ..

# Если нужен бекенд
cd бекенд
npm install
cd ..
```

---

## 🎯 Шаг 6: Откройте проект в Xcode

```bash
open ios/Runner.xcworkspace
```

---

## ✅ Готово!

Проект теперь на Mac и готов к сборке для iOS!

---

## 🔄 Если нужно обновить проект на Mac (после изменений на Windows):

### На Windows:
```bash
git add .
git commit -m "Описание изменений"
git push
```

### На Mac:
```bash
git pull
flutter pub get
cd ios && pod install && cd ..
```

---

## 💡 Альтернатива: Если нет GitHub

Можно использовать другие сервисы:
- **GitLab:** https://gitlab.com
- **Bitbucket:** https://bitbucket.org
- **Или локальный Git:** просто скопируйте папку `.git` вместе с проектом

---

## ⚡ Самый быстрый способ (если GitHub не нужен):

1. **Создайте архив проекта** (исключите `build/`, `.dart_tool/`, `node_modules/`)
2. **Загрузите в Google Drive / Dropbox / OneDrive**
3. **Скачайте на Mac и распакуйте**

Но Git - лучший вариант для разработки! 🚀
