# 🔧 Исправление проблем на Mac

## Проблема 1: CocoaPods требует более высокий deployment target

### Решение:

1. **Обновите Podfile** (уже сделано в репозитории):
   - Deployment target изменен с iOS 12.0 на iOS 13.0

2. **Выполните на Mac:**
   ```bash
   cd msngIosAndroidv2
   cd ios
   rm -rf Pods Podfile.lock
   pod install
   ```

## Проблема 2: Неправильный путь к workspace

### Решение:

Команда `open ios/Runner.xcworkspace` должна выполняться из **корневой папки проекта**, а не из папки `ios/`.

### Правильная последовательность команд:

```bash
# 1. Клонируйте репозиторий (если еще не сделано)
git clone https://github.com/Uz11ps/msngIosAndroidv2.git
cd msngIosAndroidv2

# 2. Установите Flutter зависимости
flutter pub get

# 3. Перейдите в папку ios и установите CocoaPods зависимости
cd ios
rm -rf Pods Podfile.lock  # Очистите старые зависимости
pod install
cd ..  # Вернитесь в корневую папку проекта

# 4. Откройте проект в Xcode (из корневой папки!)
open ios/Runner.xcworkspace
```

## Альтернативный способ открытия:

Если вы уже в папке `ios/`, используйте:
```bash
open Runner.xcworkspace
```

Или из любой папки используйте полный путь:
```bash
open ~/msngIosAndroidv2/ios/Runner.xcworkspace
```

## Если pod install все еще не работает:

1. **Обновите CocoaPods:**
   ```bash
   sudo gem install cocoapods
   pod repo update
   ```

2. **Проверьте версию Flutter:**
   ```bash
   flutter --version
   flutter doctor
   ```

3. **Очистите кэш:**
   ```bash
   cd ios
   rm -rf Pods Podfile.lock .symlinks
   flutter clean
   cd ..
   flutter pub get
   cd ios
   pod install
   ```

## Проверка deployment target в Xcode:

После открытия проекта в Xcode:
1. Выберите проект `Runner` в навигаторе
2. Выберите target `Runner`
3. Перейдите на вкладку **"General"**
4. Проверьте **"iOS Deployment Target"** - должно быть **13.0** или выше
5. Если нужно, измените на **13.0**

---

## ✅ После исправления:

Проект должен успешно открыться в Xcode и быть готов к сборке для TestFlight!
