# 🔐 Руководство по разрешениям в iOS приложении

## ✅ Разрешения уже настроены!

Все необходимые разрешения уже добавлены в `Info.plist` и код приложения.

---

## 📱 Как работают разрешения на iOS:

На iOS разрешения запрашиваются **автоматически** при первом использовании функции. Пользователь увидит системный диалог с запросом разрешения.

---

## 🎤 Разрешения в приложении:

### 1. Микрофон (NSMicrophoneUsageDescription)
**Когда запрашивается:**
- При первом аудио звонке
- При первом видеозвонке
- При первой записи голосового сообщения

**Текст запроса:** "Приложению необходим доступ к микрофону для записи голосовых сообщений и аудиозвонков"

### 2. Камера (NSCameraUsageDescription)
**Когда запрашивается:**
- При первом видеозвонке
- При первом выборе фото из камеры

**Текст запроса:** "Приложению необходим доступ к камере для отправки фотографий и видеозвонков"

### 3. Галерея (NSPhotoLibraryUsageDescription)
**Когда запрашивается:**
- При первом выборе фото из галереи

**Текст запроса:** "Приложению необходим доступ к галерее для выбора фотографий"

### 4. Сохранение фото (NSPhotoLibraryAddUsageDescription)
**Когда запрашивается:**
- При сохранении фото в галерею

**Текст запроса:** "Приложению необходим доступ для сохранения фотографий в галерею"

---

## 🔄 Как это работает:

1. **Пользователь открывает приложение** → Разрешения НЕ запрашиваются сразу

2. **Пользователь нажимает на аудио звонок** → Появляется системный диалог:
   ```
   "Messenger App" хочет получить доступ к микрофону
   [Не разрешать] [OK]
   ```

3. **Пользователь нажимает "OK"** → Разрешение предоставлено, звонок начинается

4. **Пользователь нажимает "Не разрешать"** → Разрешение отклонено, звонок не начинается

---

## ⚙️ Что уже реализовано в коде:

### В `lib/main.dart`:
- При запуске приложения запрашиваются базовые разрешения (микрофон, камера, уведомления)

### В `lib/screens/chat_screen.dart`:
- Перед началом звонка проверяется и запрашивается разрешение на микрофон
- Перед видеозвонком дополнительно запрашивается разрешение на камеру
- Перед записью голосового сообщения запрашивается разрешение на микрофон
- Если разрешение отклонено, показывается сообщение с кнопкой "Настройки"

---

## 🛠️ Если разрешения не запрашиваются:

### Проверка 1: Убедитесь, что Info.plist настроен правильно

В `ios/Runner/Info.plist` должны быть:
- ✅ `NSMicrophoneUsageDescription`
- ✅ `NSCameraUsageDescription`
- ✅ `NSPhotoLibraryUsageDescription`
- ✅ `NSPhotoLibraryAddUsageDescription`

### Проверка 2: Проверьте код запроса разрешений

Разрешения запрашиваются автоматически в:
- `lib/main.dart` - при запуске приложения
- `lib/screens/chat_screen.dart` - перед звонками и записью

### Проверка 3: Переустановите приложение

Если разрешения не запрашиваются:
1. Удалите приложение с устройства
2. Установите заново из TestFlight
3. Попробуйте использовать функцию (звонок, запись голосового)

---

## 📱 Для пользователей TestFlight:

### Как предоставить разрешения:

1. **При первом использовании функции:**
   - Нажмите на кнопку звонка/записи голосового
   - Появится системный диалог iOS
   - Нажмите "OK" или "Разрешить"

2. **Если разрешение было отклонено:**
   - Настройки → Messenger App → Разрешения
   - Включите нужные разрешения вручную

3. **Если разрешение не запрашивается:**
   - Удалите приложение
   - Установите заново из TestFlight
   - Попробуйте использовать функцию снова

---

## 🔍 Проверка разрешений в коде:

### Микрофон:
```dart
// Проверка статуса
final status = await Permission.microphone.status;

// Запрос разрешения
final result = await Permission.microphone.request();
```

### Камера:
```dart
// Проверка статуса
final status = await Permission.camera.status;

// Запрос разрешения
final result = await Permission.camera.request();
```

---

## ⚠️ Важно:

- Разрешения запрашиваются **только один раз** при первом использовании
- Если пользователь отклонил разрешение, нужно открыть Настройки вручную
- В коде есть обработка отклоненных разрешений с предложением открыть Настройки

---

## ✅ Все готово!

Разрешения уже настроены и будут запрашиваться автоматически при использовании функций. Пользователям TestFlight просто нужно разрешить доступ при первом использовании!
