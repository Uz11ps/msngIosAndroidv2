# 🔧 Исправление ошибки Podfile.lock

## Проблема:
"The sandbox is not in sync with the Podfile.lock. Run 'pod install' or update your CocoaPods installation."

## Решение:

Нужно запустить `pod install` в папке `ios/`.

---

## 📋 Что нужно сделать на Mac:

### Вариант 1: Через терминал (Рекомендуется)

```bash
cd msngIosAndroidv2
cd ios
pod install
cd ..
```

### Вариант 2: Если pod install не работает

```bash
cd msngIosAndroidv2
cd ios

# Очистите старые зависимости
rm -rf Pods Podfile.lock

# Переустановите
pod install --repo-update

cd ..
```

### Вариант 3: Если CocoaPods не установлен

```bash
# Установите CocoaPods (если еще не установлен)
sudo gem install cocoapods

# Обновите репозиторий
pod repo update

# Затем установите зависимости
cd msngIosAndroidv2/ios
pod install
cd ..
```

---

## ✅ После выполнения pod install:

1. **Закройте Xcode** (если открыт)

2. **Откройте проект заново:**
   ```bash
   open ios/Runner.xcworkspace
   ```
   ⚠️ **ВАЖНО:** Открывайте `.xcworkspace`, а НЕ `.xcodeproj`!

3. **Проверьте, что ошибка исчезла**

4. **Попробуйте собрать проект:**
   - Product → Clean Build Folder (Shift+Cmd+K)
   - Product → Build (Cmd+B)

---

## 🔍 Если ошибка все еще появляется:

### Проверка 1: Убедитесь, что используете .xcworkspace

```bash
# Правильно:
open ios/Runner.xcworkspace

# Неправильно:
open ios/Runner.xcodeproj  # ❌ Не используйте это!
```

### Проверка 2: Проверьте версию CocoaPods

```bash
pod --version
```

Если версия старая, обновите:
```bash
sudo gem install cocoapods
```

### Проверка 3: Очистите кэш CocoaPods

```bash
cd ios
rm -rf Pods Podfile.lock .symlinks
pod cache clean --all
pod install
cd ..
```

### Проверка 4: Проверьте Flutter зависимости

```bash
flutter clean
flutter pub get
cd ios
pod install
cd ..
```

---

## 📝 Полная последовательность команд:

```bash
# 1. Перейдите в проект
cd msngIosAndroidv2

# 2. Очистите Flutter кэш
flutter clean
flutter pub get

# 3. Перейдите в ios папку
cd ios

# 4. Очистите старые зависимости
rm -rf Pods Podfile.lock .symlinks

# 5. Установите зависимости
pod install

# 6. Вернитесь в корень проекта
cd ..

# 7. Откройте проект в Xcode
open ios/Runner.xcworkspace
```

---

## ⚠️ Важно:

- Всегда используйте `.xcworkspace` файл для открытия проекта
- После изменений в Podfile всегда запускайте `pod install`
- Если добавляете новые зависимости, сначала обновите `pubspec.yaml`, затем `flutter pub get`, затем `pod install`

---

## ✅ После исправления:

Ошибка должна исчезнуть, и проект должен успешно собираться в Xcode!
