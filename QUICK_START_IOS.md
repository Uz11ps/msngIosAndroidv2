# 🚀 Быстрый старт: Сборка для iOS и TestFlight

## ✅ Что уже готово:

1. ✅ **Podfile создан** - `ios/Podfile` настроен для iOS 12.0+
2. ✅ **Info.plist настроен** - все разрешения добавлены:
   - Микрофон (для голосовых сообщений и звонков)
   - Камера (для фото и видеозвонков)
   - Галерея (для выбора фото)
3. ✅ **Разрешения в коде** - запросы разрешений работают на iOS и Android
4. ✅ **Визуальная совместимость** - приложение использует Material Design, который работает одинаково на обеих платформах

---

## 📋 Пошаговая инструкция:

### Шаг 1: На Mac откройте проект

```bash
# Перейдите в папку проекта
cd путь/к/проекту

# Установите зависимости
cd ios
pod install
cd ..
flutter pub get
```

### Шаг 2: Откройте в Xcode

```bash
open ios/Runner.xcworkspace
```

⚠️ **ВАЖНО:** Открывайте `.xcworkspace`, а НЕ `.xcodeproj`!

### Шаг 3: Настройте Signing

1. В Xcode выберите проект `Runner` слева
2. Выберите target `Runner`
3. Вкладка **"Signing & Capabilities"**
4. ✅ Включите **"Automatically manage signing"**
5. Выберите вашу **Team** (Apple Developer Account)
6. Проверьте **Bundle Identifier** (должен быть уникальным)

### Шаг 4: Проверьте версию

В `pubspec.yaml`:
```yaml
version: 1.0.0+1
```

Для каждого нового билда увеличивайте `+1` → `+2` → `+3` и т.д.

### Шаг 5: Соберите Archive

1. В Xcode выберите **"Any iOS Device"** (НЕ симулятор!)
2. **Product** → **Archive**
3. Дождитесь завершения (5-10 минут)

### Шаг 6: Загрузите в TestFlight

1. В окне **Organizer** выберите ваш архив
2. Нажмите **"Distribute App"**
3. Выберите **"App Store Connect"**
4. Выберите **"Upload"**
5. Следуйте инструкциям

### Шаг 7: В App Store Connect

1. Войдите на https://appstoreconnect.apple.com/
2. **My Apps** → выберите приложение
3. **TestFlight** → дождитесь обработки билда (10-30 минут)
4. Добавьте тестировщиков и отправьте приглашения

---

## 🔍 Проверка перед отправкой:

- [ ] Приложение запускается на симуляторе/устройстве
- [ ] Авторизация работает
- [ ] Сообщения отправляются и получаются
- [ ] Голосовые сообщения записываются
- [ ] Звонки работают (аудио и видео)
- [ ] Разрешения запрашиваются корректно
- [ ] Все экраны отображаются правильно

---

## 📚 Подробные инструкции:

- **TESTFLIGHT_GUIDE.md** - полная инструкция по TestFlight
- **IOS_BUILD_CHECKLIST.md** - детальный чеклист для сборки

---

## 🐛 Если что-то не работает:

### Проблема: "No such module 'Flutter'"
```bash
cd ios
pod deintegrate
rm -rf Pods Podfile.lock
pod install --repo-update
cd ..
flutter clean
flutter pub get
```

### Проблема: "Code signing error"
- Проверьте Team в Xcode
- Убедитесь, что у вас активная подписка Apple Developer

### Проблема: "Agora SDK not found"
```bash
cd ios
pod install --repo-update
```

---

## ✨ Особенности iOS:

1. **Разрешения:** На iOS разрешения запрашиваются автоматически при первом использовании функции
2. **Визуал:** Material Design работает одинаково на iOS и Android
3. **Звонки:** Agora SDK поддерживает iOS 12.0+
4. **Уведомления:** На iOS уведомления запрашиваются автоматически

---

## 🎯 Готово к сборке!

Все настройки выполнены. Проект готов для сборки и загрузки в TestFlight.

**Удачи! 🚀**
